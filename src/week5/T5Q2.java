package week5;

public class T5Q2 {
    
    /**
     * else{
            Node<E> temp = head; // new 'head' equal to node 'temp'
            for(int i=0; i<index; i++){ // for loop to iterate until a node before the index
            temp = temp.next; // moving into the next node
        }
            element = temp.element; 
            temp.next.prev = temp.prev;
            temp.prev.next = temp.next;
            temp.next = null;
            temp.prev = null;
        size --;
        }

        Based on the source code above, assume the index given is 3

        a) Explain what the lines of code do from line 2-11.
        :- removing a node based on the index entered by user 
        
        b) Draw the nodes for lines 7 - 10
     */
}
