1.method f(0)

public static int f(int n) {
		if(n == 1)
			return n;
		else 
			return n * f(n - 1);
	}

the problem :-
 the code will enter an infinite recursive loop, causing the program to run indefinitely or potentially leading to a stack overflow error.

2.method f()

public static int f(int n) {
if (n == 0)
return n;
else
return f(n+1) + n;
}

the problem :-
the code will enter an infinite recursive loop, causing the program to run indefinitely or potentially leading to a stack overflow error.

